import { CountryLinkDirective } from './country-link.directive';

describe('CountryLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryLinkDirective();
    expect(directive).toBeTruthy();
  });
});
